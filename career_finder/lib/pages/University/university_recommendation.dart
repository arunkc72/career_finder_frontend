import 'package:flutter/material.dart';
class UniversityRecommendation extends StatelessWidget {
const UniversityRecommendation({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}